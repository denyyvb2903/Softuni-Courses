function stringLength (text1, text2, text3) {

    let total = text1.length + text2.length + text3.length

    console.log(total);
    console.log(Math.floor(total / 3));
}
stringLength('chocolate', 'ice cream', 'cake')